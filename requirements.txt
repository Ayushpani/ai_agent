# Portfolio Intelligence Agent — pinned dependencies.
# Zero paid-API-spend design (doc §Executive Summary): every LLM call
# this stack makes targets a free-tier endpoint (OpenRouter, Cloudflare
# Workers AI) via LiteLLM; nothing here requires a paid model key.

# Orchestration
fastapi==0.141.1
uvicorn==0.52.4
langgraph==1.2.11
litellm==1.100.1
pydantic==2.13.5

# Query execution / data
duckdb==1.5.5
pandas==3.0.5
numpy==2.4.6
pyarrow==25.0.1
sqlglot==30.18.0
PyYAML==6.0.3

# Statistics / forecasting
scipy==1.17.1
statsmodels==0.15.0
# pmdarima is optional (auto-ARIMA candidate in app/tools/forecast.py);
# install separately if your platform has a working C/Cython toolchain:
# pmdarima==2.0.4

# Charting / Excel
plotly==7.0.0
XlsxWriter==3.2.9

# Interface
chainlit==2.12.0

# Observability
structlog==26.1.0

# Testing
pytest==9.1.1
httpx==0.28.1
